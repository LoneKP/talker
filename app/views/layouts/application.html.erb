<!DOCTYPE html>
<html>
  <head>
    <title>Talker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://fonts.googleapis.com/css?family=Material+Icons|Material+Icons+Outlined|Material+Icons+Two+Tone|Material+Icons+Round|Material+Icons+Sharp" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  
  <body data-talk-id="<%= @talk&.id %>" class="bg-orange-50 font-mono">
    <div class="flex-col mx-auto lg:max-w-screen-xl">
    <% if Rails.env.development? %>
      <p>current visitor: <%= Current.visitor&.id %> | name: <%= Current.visitor&.name %> | Facilitator? <%= Current.visitor&.facilitator?(@talk) %></p>
    <% end %>
      <%= yield %>
    </div>
    <%= render partial: "shared/flash" %>
  </body>
</html>
